name: hearme_ai
description: "AI Teman Curhat Anonim"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_dotenv: ^5.1.0
  flutter_riverpod: ^2.5.1 
  flutter_markdown: ^0.7.3 
  google_fonts: ^6.2.1      

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - .env   
